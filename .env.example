# E2E Test Runner Environment Variables

# Allow test execution (set to 'true' to enable)
NEXT_PUBLIC_E2E_ALLOW_EXECUTION=true

# Staging environment URL (for running tests)
E2E_STAGING_URL=https://gloz-dev.gloground.com

# Hub environment URL (for Hub-based auth tests)
E2E_HUB_URL=http://localhost:3000

# Playwright base URL (overrides default)
# PLAYWRIGHT_BASE_URL=http://localhost:3001

# Test user credentials (for auth setup)
E2E_TEST_EMAIL=your-test-email@example.com
E2E_TEST_PASSWORD=your-test-password

# Hub authentication (for auth-hub.setup.ts)
HUB_URL=https://hub-dev.gloground.com

# Optional: Node environment
NODE_ENV=development
